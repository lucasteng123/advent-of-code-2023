import assert from 'assert';

import { Day } from '../src/days/day0';

const input = 
``;

describe('Day 0', ()=>{
  it('should run part 1', ()=>{
    assert.equal(Day.part1(input),'');
  });
  it('should run part 2', ()=>{
    assert.equal(Day.part2(input),'');
  });
});