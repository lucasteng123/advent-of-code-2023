import assert from 'assert';

import { Day } from '../src/days/day0';
import { parseInput } from '../src/utils/inputParsing';

const input = 
``;

describe.skip('Day 0', ()=>{
  it('should run part 1', ()=>{
    assert.equal(Day.part1(parseInput(input)),'');
  });
  it('should run part 2', ()=>{
    assert.equal(Day.part2(parseInput(input)),'');
  });
});